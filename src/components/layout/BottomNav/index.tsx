import BottomNavItem from './ui/BottomNavItem';
import BottomNavMain from './ui/BottomNavMain';

const BottomNav = Object.assign(BottomNavMain, {
  Item: BottomNavItem,
});

export default BottomNav;
